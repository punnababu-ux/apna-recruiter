"use client"

/**
 * /onlyrounds/tokens — Poneglyph design-system showcase rendered under the
 * OnlyRounds theme. Full-bleed route (no product shell) so the token grid,
 * color ramps, and component gallery can breathe.
 *
 * The product surface itself lives at /onlyrounds/jobs and below, wrapped
 * in the sidebar shell from `(product)/layout.tsx`.
 */

import Home from "../../page"

export default function OnlyRoundsTokensPage() {
  return <Home />
}
